#include<stdio.h>
int main()
{
    int length, area;
    printf("Please input the length:");
    scanf("%d", &length);
    area= length*length;
    printf("Area is: %d", area);
    return 0;
}
